# Copy to `.env` for local development (do NOT commit `.env`)
# On Vercel, set these in Project → Settings → Environment Variables (Production + Preview).

# Vercel-native backend (Postgres + Blob)

# Auth / security (server-side)
SECRET_KEY=

# Admin name allowlist (comma-separated). Default is "ataliba" if not set.
ADMIN_NAMES=ataliba

# Optional CORS
ALLOWED_ORIGINS=

# Provided by Vercel integrations:
# - Vercel Postgres → POSTGRES_* / POSTGRES_URL*
# - Vercel Blob → BLOB_READ_WRITE_TOKEN


